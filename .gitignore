# Build output (created by scripts/build_unsigned_dmg.sh)
build/

# Distributables — shipped via GitHub Releases, not committed
*.dmg

# Notarization config (local-only)
.notary-issuer

# Xcode
DerivedData/
*.xcuserstate
xcuserdata/

# macOS
.DS_Store
